/**
 * HR module — staff roster & payroll
 *
 * Future additions:
 *   hooks/     → useShiftPlanner, usePayrollCalculator
 *   types/     → Employee, Shift, PayrollEntry, Deduction
 *   services/  → HR/payroll API calls
 *   components/ → RosterCalendar, PayslipCard, EmployeeProfile
 */

export { default as StaffRoster } from './pages/StaffRoster';
export { default as Payroll }     from './pages/Payroll';
