/**
 * Dashboard module — top-level overview & KPIs
 *
 * Future additions per-module:
 *   hooks/     → useDashboardStats, useSalesSummary
 *   types/     → DashboardWidget, KpiCard
 *   components/ → StatWidget, SalesChart
 */

export { default as Dashboard } from './pages/Dashboard';
